---
description: >
  Close the learning loop. Default mode captures lessons from a feature's qa/fix
  cycle, classifies them into the error taxonomy, and promotes rules (gated) into
  .opencode/rules/agents/. --consolidate runs the Verify & Prune pass (dedup,
  retire, graduate validated rules into the standards, gate mechanical rules,
  enforce the per-agent budget). Governed by .opencode/rules/LEARNING-LOOP.md.
---

Read `.opencode/skills/improve/SKILL.md` and follow its instructions. Read
`.opencode/rules/LEARNING-LOOP.md` first — it owns the rule schema, the maturity
ladder, and the promotion/verify/prune policies.

$ARGUMENTS

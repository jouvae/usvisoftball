---
description: >
  Enter live, full-stack Prototyping Mode — design a feature (UI-first) directly
  in the running app, against the real backend, in tight visible loops.
  Orchestrates architect, nextjs-tester, nextjs-implementer, and (only when
  needed) go-tester / go-implementer. One slice at a time; never advances past a
  slice until the user confirms it in the browser.
---

Read `.opencode/commands/proto.md` and follow its instructions.

$ARGUMENTS

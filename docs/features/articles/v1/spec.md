admin users will need to be able to create and publish articles. this finctionality has already been implemented but we need to now make it more advances.
- update the article schema so that it can handle dynamic content. the article body will need to be something like

